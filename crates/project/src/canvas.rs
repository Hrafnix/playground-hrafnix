/// Dimensions of a canvas in canvas units.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct CanvasDimensions {
    /// Canvas width.
    pub width: u16,
    /// Canvas height.
    pub height: u16,
}
